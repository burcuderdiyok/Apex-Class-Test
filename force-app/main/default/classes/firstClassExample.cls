public class firstClassExample {
    public static void createList(integer k){
        List<string> stdList=New List<String> ();
        for (integer i=1; i<=k; i++){
            stdList.add('F'+i);
        }
        system.debug(stdList);
    }
    
}